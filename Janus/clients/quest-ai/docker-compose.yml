version: "3.9"

services:
  quest-ai:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./lore:/app/lore
    restart: unless-stopped
